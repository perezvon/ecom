{
  "name": "ecom",
  "version": "1.0.0",
  "description": "",
  "author": "perezvon",
  "license": "ISC",
  "scripts": {
    "start": "cd server && node server",
    "dev": "concurrently \"cd ./ecom-server && node --inspect server\" \"cd ./ecom-client && react-scripts start\"",
    "heroku-postbuild": "cd ./ecom-server && yarn && cd ../ecom-client && yarn && yarn run build"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
